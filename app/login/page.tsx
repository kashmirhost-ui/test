'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function Login(){const [u,setU]=useState('');const[p,setP]=useState('');const[e,setE]=useState('');const r=useRouter();async function go(ev:any){ev.preventDefault();const x=await fetch('/api/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:u,password:p})});if(x.ok)r.push('/dashboard');else setE('Invalid username or password');}return <main className="login card"><h1>WiFi RADIUS Manager</h1><p>Administrator login</p><form onSubmit={go}><input placeholder="Username" value={u} onChange={e=>setU(e.target.value)} required/><input placeholder="Password" type="password" value={p} onChange={e=>setP(e.target.value)} required/><button className="btn">Sign in</button>{e&&<div className="error">{e}</div>}</form></main>}
