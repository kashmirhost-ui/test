import type { NextPage } from 'next';

const Home: NextPage = () => (
  <main style={{fontFamily:'system-ui',padding:40}}>
    <h1>WiFi RADIUS Manager</h1>
    <p>Next.js RADIUS management dashboard.</p>
  </main>
);

export default Home;
