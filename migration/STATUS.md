# Migration status

The repository is being migrated from the uploaded PHP RADIUS Manager to Next.js. The initial scaffold includes the Next.js app manifest, environment template, and RADIUS UDP detection helper. Remaining PHP modules should be ported incrementally with tests.
